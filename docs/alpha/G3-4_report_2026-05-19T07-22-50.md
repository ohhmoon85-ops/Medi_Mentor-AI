# G3-4 종합 보고서 (마감판) — 메트릭 N1 Vercel Blob 마이그레이션 완료 (α 전략)

## §1 발주 메타

| 항목 | 값 |
|---|---|
| 발주 ID | G3-4 (메트릭 N1 Vercel Blob 마이그레이션) — **마감판** |
| 실행 시작 | 2026-05-19T06:58:52 KST |
| 단계 3-α 마감 | 2026-05-19T07:01:54 KST |
| 마감판 산출 | 2026-05-19T07:22:50 KST |
| 적용 정책 | K-1 / K-2 / K-3 / K-5 / O-2 / P-1 / P-2 / §1.8 / §8.6 / §8.7 |
| 선행 보고 (이력) | `docs/alpha/G3-4_report_2026-05-19T07-01-54.md` (단계 3-α 마감판) |

### §1.1 갱신 사유

단계 3-α 마감판 산출 후 사용자(문형철) ACK `α-OK, (α) 채택` 회신.
ACK 내용:
- (α) 메트릭 타입별 분리 전략 즉시 진행
- 자유 텍스트 PII 처리 (M1.comment / M2.mismatch_reason / M3.er_diagnosis) 는 LAUNCH-0 P-2(d) 후보로 기록, wire-in 본격화 직전 G3-5 발주

본 마감판은 단계 3-β (2건 마이너 패치) + W-4 회귀 검증 통과 후 정본.

---

## §2 W-1 단계 3-α 사전 조사 결과 (변동 없음)

§단계 3-α 마감판 §2 와 동일. 핵심 사실:

- 메트릭 모듈 baseline: `app/api/care/metrics/route.ts` hash `90894a553d1fdfbb` (변경 대상)
- 프로덕션 fs 사용 모듈 1건만 잔여 (G3-3 마감 후)
- 메트릭 4 타입 자유 텍스트 PII 위험 식별 (현재 자동 적재 0건)
- **보호 23건 교집합 = 0건**
- 라우팅 ↔ 메트릭 양방향 import 0건
- G3-3 동의 모듈 ↔ 메트릭 양방향 import 0건

---

## §3 W-2 단계 3-α 마이그레이션 설계 (적용 완료)

§단계 3-α 마감판 §3 와 동일 설계. 단계 3-β 적용 시 미세 조정 0건 (설계 그대로 적용 성공).

| 항목 | 단계 3-α 설계 | 단계 3-β 실측 |
|---|---|---|
| 동시성 전략 | (α) 메트릭 타입별 분리 — `metrics/<type>.jsonl` | **동일 적용** |
| Blob SDK API | `list({prefix})` + `fetch(target.url)` + `put()` | **동일 적용** |
| Content-Type | `application/x-ndjson` (NDJSON 명시) | **동일 적용** |
| Access mode | `'public'` + `addRandomSuffix: false` (G3-3 정합) | **동일 적용** |
| 변경 대상 파일 | 2건 (route.ts + .env.example) | **2건 실측** |
| 메트릭 스키마 | metrics-types.ts / metrics-collector.ts 1bit 보존 | **1bit 보존 실측** |

---

## §4 W-3 단계 3-β 패치 내용 (마감)

### §4.1 변경 파일 2건

| # | 파일 | 변경 종류 | 실측 라인 변화 |
|---|---|---|---|
| 1 | `app/api/care/metrics/route.ts` | fs (`appendFileSync`/`mkdirSync`) → Blob (`list`+fetch+`put`) + 헤더 주석 갱신 | +27 -8 |
| 2 | `.env.example` | `BLOB_READ_WRITE_TOKEN` 주석에 G3-4 메트릭 재사용 1줄 추가 | +1 |

### §4.2 코드 diff 요약

**패치 1 — import 변경**:
```diff
- import { appendFileSync, mkdirSync } from 'fs'
- import path from 'path'
+ import { put, list } from '@vercel/blob'
```

**패치 2 — 저장 로직 마이그레이션** (route.ts:37-47 → 38-65):
```diff
-  // JSONL 파일 저장
-  try {
-    const dateStr = new Date().toISOString().slice(0, 10)
-    const logsDir = path.join(process.cwd(), 'logs', 'metrics')
-    mkdirSync(logsDir, { recursive: true })
-    const logPath = path.join(logsDir, `${dateStr}.jsonl`)
-    appendFileSync(logPath, JSON.stringify(record) + '\n', 'utf8')
-  } catch (e) {
-    console.error('[metrics] 파일 저장 실패:', e)
-  }
+  // Vercel Blob NDJSON append (read-modify-write)
+  // 동시성 위험: 동일 type 동시 POST 시 손실 가능 — 알파 5명 TPS << 1 허용 위험
+  try {
+    const pathname = `metrics/${record.payload.metric_type}.jsonl`
+    const newLine = JSON.stringify(record) + '\n'
+
+    // 기존 NDJSON 읽기 (없으면 빈 문자열)
+    let existing = ''
+    try {
+      const result = await list({ prefix: pathname })
+      const target = result.blobs.find((b) => b.pathname === pathname)
+      if (target) {
+        const res = await fetch(target.url)
+        if (res.ok) existing = await res.text()
+      }
+    } catch {
+      // 첫 적재 — 새 Blob 생성
+    }
+
+    await put(pathname, existing + newLine, {
+      access:          'public',
+      addRandomSuffix: false,
+      contentType:     'application/x-ndjson',
+    })
+  } catch (e) {
+    console.error('[metrics] Blob 저장 실패:', e)
+  }
```

**패치 3 — .env.example 가이드 보강**:
```diff
 # 누락 시 submitConsent 의 put() 호출이 try/catch 로 흡수되어 동의 흐름 무영향.
+# G3-4 추가: 메트릭 N1 (POST /api/care/metrics) 동일 토큰 재사용 — pathname = metrics/<metric_type>.jsonl (NDJSON)
 BLOB_READ_WRITE_TOKEN=
```

### §4.3 자체 보정 0건

G3-1 / G3-3 의 첫 시도 보정 사례와 달리, 본 발주에서는 **자체 보정 0건**. 단계 3-α 설계가 단계 3-β 실측과 1bit 일치 (Blob SDK API 시그니처 사전 검증 — `head`/`get`/`list`/`put` 모두 export 확인됨).

---

## §5 W-4 회귀 검증 결과

### §5.1 TypeScript 타입 체크

```
$ npx tsc --noEmit
(빈 출력)
```

**0 오류** ✓.

### §5.2 단위 테스트 (8 파일, 회귀 0건)

| 파일 | 결과 |
|---|---|
| `lib/care/alpha-validation/alpha-validator.test.ts` | 16/16 PASS |
| `lib/care/metrics/metrics-collector.test.ts` | 15/15 PASS |
| `lib/care/routing-policy/routing-policy-table.test.ts` | 63/63 PASS |
| `lib/care/triage-level-converter.test.ts` | 35/35 PASS |
| `lib/care/policy-evaluator.test.ts` | 42/42 PASS |
| `lib/care/real-routing-adapter.test.ts` | 150/150 PASS |
| `components/care/feedback/feedback-entry-button.test.tsx` | 18/18 PASS |
| `components/care/consent/consent-form.test.tsx` | 26/26 PASS (G3-3 비트 동등 — hash `34a3fcfc` 보존) |
| **누계** | **365 / 365 PASS** |

### §5.3 G2-5 28 시나리오 비트 동등성

```
Total    : 28
Passed   : 28
Type II  : 0
Routing% : 100.0%
```

**G3-3 마감 보고** (`2026-05-18T22-22-22-584Z.json`) **vs G3-4 마감 보고** 비트 동등성 검증:

```
G3-4 전·후 G2-5 비트 동등: ✅
```

`run_timestamp` 제외 모든 필드 1bit 일치. **라우팅 모듈 무영향 정합 확정**.

### §5.4 G3-3 동의 모듈 hash 보존 검증

| 파일 | 마감판 baseline (G3-3) | G3-4 마감 시점 | 보존 |
|---|---|---|---|
| `components/care/consent/consent-form.tsx` | `f14a77f4` | `f14a77f4` | ✓ |
| `components/care/consent/consent-form.test.tsx` | `34a3fcfc` | `34a3fcfc` | ✓ |

발주문 §3 #4 "동의 모듈(G3-3) 수정 금지" 정합 ✓.

### §5.5 환경 변수 누락 시 동작

`BLOB_READ_WRITE_TOKEN` 미설정 환경 (예: 본 로컬 검증):
- `list()` 또는 `put()` 호출 시 `BlobError` 발생
- 기존 `try/catch` 가 흡수 → POST API 응답 정상 (`200 ok: true`)
- `console.error` 로 디버깅 메시지 출력 (기존 동작 보존)
- 사용자 경험: 메트릭 적재만 실패, 라우팅 결과 페이지·동의 흐름 모두 정상

**G2-5 검증은 메트릭 모듈 무관**(어댑터 + 라우팅 모듈만 호출). 즉 Blob 환경 변수 없이도 G2-5 정상 통과.

---

## §6 보호 파일 23건 hash 시작·마감판 비교

baseline: 2026-05-19T06:58:52 KST (발주 시작)
end-state: 2026-05-19T07:22:50 KST (마감판 산출)

| # | 파일 | baseline | end-state | 변경 |
|---|---|---|---|---|
| 1 | `lib/specialty/recommendation-engine.ts` | `868a7553` | `868a7553` | 없음 |
| 2 | `lib/specialty/llm-fallback.ts` | `dabba85f` | `dabba85f` | 없음 |
| 3 | `lib/specialty/family-demographics.ts` | `b8e52ce5` | `b8e52ce5` | 없음 |
| 4 | `lib/specialty/external-map-link.ts` | `640c7812` | `640c7812` | 없음 |
| 5 | `lib/specialty/recommendation-message.ts` | `a57b9e05` | `a57b9e05` | 없음 |
| 6 | `lib/specialty/night-weekend-mapping.ts` | `b09c9cc7` | `b09c9cc7` | 없음 |
| 7 | `lib/specialty/night-weekend-message.ts` | `fa6ae854` | `fa6ae854` | 없음 |
| 8 | `lib/specialty/symptom-list-draft.ts` | `06c688f3` | `06c688f3` | 없음 |
| 9 | `lib/care/symptom-journal.ts` | `2ff523d8` | `2ff523d8` | 없음 |
| 10 | `lib/care/medication-list.ts` | `6d1a663d` | `6d1a663d` | 없음 |
| 11 | `lib/care/visit-checklist.ts` | `818ded3e` | `818ded3e` | 없음 |
| 12 | `lib/care/ui-tokens.ts` | `d0025054` | `d0025054` | 없음 |
| 13 | `lib/shared/llm-client.ts` | `ad1cde61` | `ad1cde61` | 없음 |
| 14 | `components/specialty/specialty-recommendation-card.tsx` | `2310ced0` | `2310ced0` | 없음 |
| 15 | `components/specialty/specialty-recommendation-card-wrapper.tsx` | `727008b9` | `727008b9` | 없음 |
| 16 | `components/specialty/night-weekend-info-card.tsx` | `8a9a2616` | `8a9a2616` | 없음 |
| 17 | `components/care/symptom-journal-card.tsx` | `681321d2` | `681321d2` | 없음 |
| 18 | `app/(care)/care/visit-prep/page.tsx` | `19969c2b` | `19969c2b` | 없음 |
| 19 | `lib/care/routing-policy/routing-policy-table.ts` | `3b7d9c05` | `3b7d9c05` | 없음 |
| 20 | `lib/care/routing-policy/routing-policy-loader.ts` | `b28f2e70` | `b28f2e70` | 없음 |
| 21 | `lib/care/triage-level-converter.ts` | `a7b90529` | `a7b90529` | 없음 |
| 22 | `lib/care/policy-evaluator.ts` | `cc656dbf` | `cc656dbf` | 없음 |
| 23 | `lib/care/real-routing-adapter.ts` | `425a403b` | `425a403b` | 없음 |

**판정**: 23/23 무변경.

---

## §7 자가 패치 카운트 + 후속 발주 권고

### §7.1 카운트

| 구분 | 카운트 | 비고 |
|---|---|---|
| 구현 기존 파일 자가 패치 | **0건** | 보호 23건 + G3-3 동의 모듈 + 라우팅 모듈 전부 무수정 |
| 마이너 패치 (발주 인가) | 2건 | route.ts + .env.example |
| 자체 보정 (별도 카운트) | **0건** | 단계 3-α 설계가 단계 3-β 실측과 1bit 일치 (Blob API 시그니처 사전 검증) |
| 신규 파일 | 2건 | 단계 3-α 마감판 + 본 마감판 (이력 보존용) |

**누계**: 구현 기존 파일 자가 패치 0건 연속 **16회** 달성.

진행 이력 (사이클별):
1. M3-3 → 2. M3-4-final → 3. M5 → 4. M6 → 5. M4 → 6. C1 → 7. C1.5 → 8. C3 → 9. G1 → 10. G2 → 11. G3-pre → 12. G3-1 (중간판) → 13. G3-1 마감판 → 14. G3-2 → 15. G3-3 → **16. G3-4 (메트릭 N1 Blob 마이그레이션 마감)**

### §7.2 후속 발주 권고

| 후보 | 트리거 시점 | 분류 | 사용자 ACK 명시 |
|---|---|---|---|
| **G3-5** 자유 텍스트 PII 보호 (M1.comment / M2.mismatch_reason / M3.er_diagnosis) | 외부 폼 wire-in 본격화 직전 | 마이너 패치 또는 별도 | **★ ACK 수용** (LAUNCH-0 P-2(d) 후보로 기록) |
| **LAUNCH-0 P-2(d)** 외부 메트릭 서비스 (Sentry/PostHog/Better Stack) | 알파 5명 운영 결과 + Blob 비용·성능 측정 후 | 구조 재설계 | (보고서 §7.3 권고) |
| **v14 인계** 작성 | G3-4 마감 + ALPHA-0 진입 직전 | 문서 | 다음 사이클 |

---

## §8 ALPHA-0 §3 게이트 영향 (마감판)

| 게이트 | G3-3 시점 | G3-4 단계 3-α | G3-4 마감판 |
|---|---|---|---|
| C-5.2.4 동의 영속 저장 | PASS + Vercel serverless | (변동 없음) | (변동 없음) |
| C-6.1 N1 메트릭 가동 | PASS (페이지 wiring, 로컬 파일 적재) | (설계 마감) | **PASS + Vercel serverless 호환** ✅ |
| 그 외 게이트 | 변동 없음 | (변동 없음) | (변동 없음) |

**ALPHA-0 진입 가능** 판정 유지. capture_log v2 §7.3 박스 체크 그대로 유효. **Vercel 배포 시 모든 영속 저장(동의 + 메트릭) 정상 동작 보장**.

---

## §9 통과 항목 / 미통과 항목 종합

| 항목 | 결과 |
|---|---|
| W-1 사전 조사 (보호 23건 교집합 0건 확인) | **PASS** |
| W-2 설계 (변경 2건 + 라우팅·동의 격리 + 스키마 보존 + 동시성 평가) | **PASS** |
| W-3 단계 3-β 패치 (2건 마이너, 자체 보정 0건) | **PASS** |
| W-4 회귀 검증 (tsc 0 / 365/365 / G2-5 비트 동등 / G3-3 consent 비트 동등 / 보호 23건) | **PASS** |
| 자가 패치 0건 연속 16회 | **달성** |
| 메트릭 스키마 1bit 보존 | **달성** (metrics-types.ts / metrics-collector.ts 무수정) |
| 라우팅 무영향 | **달성 (비트 동등)** |
| G3-3 동의 모듈 무영향 | **달성 (hash 보존)** |

---

## §10 첨부 자료

| 자료 | 경로 |
|---|---|
| 본 발주문 | (본 채팅 인라인 G3-4 발주문) |
| 본 마감판 (정본) | `docs/alpha/G3-4_report_2026-05-19T07-22-50.md` |
| 단계 3-α 마감판 (이력) | `docs/alpha/G3-4_report_2026-05-19T07-01-54.md` |
| G2-5 직전 보고 (G3-3 마감 시점) | `docs/alpha/validation_reports/2026-05-18T22-22-22-584Z.json` |
| G2-5 G3-4 마감 시점 보고 | `docs/alpha/validation_reports/*` (최신) |
| 선행 G3-3 마감 (Blob 마이그레이션 패턴) | `docs/alpha/G3-3_report_2026-05-18T06-24-28.md` |
| 선행 G3-2 W-4 (모듈 식별 출처) | `docs/alpha/G3-2_report_2026-05-17T06-45-11.md` |
| capture_log v2 | `docs/alpha/live_verification/ALPHA-0_track2_capture_log.md` |
| v13 인계 문서 | `docs/planning/medimentor_프로젝트_인계문서_v13.md` |
| 변경 파일 #1 | `app/api/care/metrics/route.ts` (fs → @vercel/blob) |
| 변경 파일 #2 | `.env.example` (`BLOB_READ_WRITE_TOKEN` G3-4 주석 추가) |

---

**G3-4 마감판 종결.**

다음 단계 사용자 판단 사항:
- **v13 §5.1 단계 2 진입** — 알파 5명 후보 확정 + 동의 절차 실행 (Vercel serverless 영속 저장 환경 완비 상태)
- **v14 인계 문서 작성** — G3-pre / G3-1 / G3-2 / G3-3 / G3-4 마감 시점 + ALPHA-0 진입 직전 정합 보존
- **GitHub 푸시** — G3-3 + G3-4 마이그레이션 + capture_log v2 일괄 커밋
